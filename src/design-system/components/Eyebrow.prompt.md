The signature section kicker — wide-tracked uppercase JetBrains Mono in dim lime, led by a short lime rule. Place above every section title.

```jsx
<Eyebrow>Components · AI-Core / AIOps</Eyebrow>
<h2 className="sec-title">Putting LLMs to work in production SRE workflows</h2>
```

Copy is lowercase/sentence-case content rendered uppercase by the component, with a `·` separating qualifiers (brand convention).
