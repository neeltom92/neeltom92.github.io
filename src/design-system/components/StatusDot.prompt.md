The brand's heartbeat — a pulsing electric-lime dot with a phosphor glow. Pair with a mono label for an "all systems operational" status line.

```jsx
<StatusDot label="all systems operational" />
<StatusDot tone="amber" pulse={false} />
```

`tone`: `accent` (lime) · `amber` (open-source signal). Set `pulse={false}` inside panel headers where a static dot reads better.
